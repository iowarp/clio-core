[package]
name = "clio-run-task"
version = "0.1.0"
edition = "2021"
license = "BSD-3-Clause"
repository = "https://github.com/iowarp/clio-core"
description = "context-runtime Task: the base task record, its trait surface, and RunContext (issue #756)"

[lib]
name = "clio_run_task"

[dependencies]
clio-run-types = { path = "../clio-run-types" }
ctp-types = { path = "../../../context-transport-primitives/rust/ctp-types" }

# Standalone workspace during development, matching clio-run-types and the
# CTP library-wrapper crates; folds into a context-runtime workspace at
# integration.
[workspace]
